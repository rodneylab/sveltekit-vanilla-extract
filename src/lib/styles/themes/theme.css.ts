import { createTheme } from '@vanilla-extract/css';

export const [theme, themeVars] = createTheme({
  colour: {
    primary: '#f00',
    secondary: '#f00',
    alternative: '#f00',
    light: '#f00',
    dark: '#f00',
  },
});
